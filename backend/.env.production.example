# 生产环境配置模板
# 部署时复制为 .env 并填写 DATABASE、JWT_SECRET 后使用: cp .env.production.example .env

NODE_ENV=production
PORT=8888

# 必填：MongoDB 连接串
DATABASE=mongodb+srv://用户名:密码@cluster.xxx.mongodb.net/数据库名?retryWrites=true&w=majority

# 必填：JWT 密钥，请改为随机长字符串
JWT_SECRET=请替换为随机密钥

OPENSSL_CONF=/dev/null
PUBLIC_SERVER_FILE=https://erp.olajob.cn/
ALLOWED_ORIGINS=https://erp.olajob.cn

# 可选：邮件、AI、PDF
# RESEND_API=re_xxx
# OPENAI_API_KEY=sk-xxx
# GOTENBERG_URL=http://gotenberg:3000
